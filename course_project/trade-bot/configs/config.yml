server:
  port: "8000"
  websocket:
    readBufferSize: 1024
    writeBufferSize: 1024
    checkOrigin: true

client:
  url: "http://localhost:8000"
  wsurl : "ws://localhost:8000"

postgreDatabase:
  host: "localhost"
  port: "5432"
  username: "postgres"
  dbname:  "postgres"
  sslmode: "disable"

redisDatabase:
  port: "6379"

kraken:
  apiurl: "https://demo-futures.kraken.com"

krakenWS:
  requests:
    writeWaitInSeconds: 10
    pongWaitInSeconds: 60
    pingPeriodInSeconds: 10
    maxMessageSize: 512
  kraken:
    wsapiurl: "wss://demo-futures.kraken.com/ws/v1"